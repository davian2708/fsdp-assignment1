version: '3.8'
services:
  ai-agent-engine:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - AI_PROVIDER=${AI_PROVIDER}
      - SECRET_KEY=${SECRET_KEY}
